install nodejs then run "npm install [libraryname]" to install libraries
    install socketio and shelljs

run 'node app.js to start the app

The application will be available in any browser at 'localhost:1234'
